extends FlyEvent


func _init() -> void:
	event_title = "Empty Space";
	event_text = "Space is mostly empty, so the odds of nothing happening should be pretty high, right?";
	event_image = preload("res://assets/graphics/events/ev_nothing.png");
